#include <bits/stdc++.h>

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	
	string s;
	int cnt = 0;
	while (getline(cin, s))
	{
		cnt++;
	}
	cout << cnt;

	return 0;
}
