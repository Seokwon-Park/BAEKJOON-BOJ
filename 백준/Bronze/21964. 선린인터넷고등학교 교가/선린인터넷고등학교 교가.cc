#include <bits/stdc++.h>

using namespace std;

using ll = long long;

char board[105][105];

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	int n;
	cin >> n;
	string s;
	cin >> s;
	cout << s.substr(s.size() - 5);

	return 0;
}