#include <bits/stdc++.h>

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);

	string fan = ":fan:";
	string id;
	cin >> id;
	id = ':' + id + ':';
	cout << fan << fan << fan << '\n';
	cout << fan << id << fan << '\n';
	cout << fan << fan << fan << '\n';
	

	return 0;
}