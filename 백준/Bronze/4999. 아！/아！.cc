#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	
	string a, b;
	cin >> a >> b;

	if (a.length() >= b.length())
	{
		cout << "go";
	}
	else
	{
		cout << "no";
	}

	return 0;
}
