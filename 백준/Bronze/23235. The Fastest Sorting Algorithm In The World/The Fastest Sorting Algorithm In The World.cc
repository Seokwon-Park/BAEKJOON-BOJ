#include <bits/stdc++.h>

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	
	int ca = 1;
	while (true)
	{
		int n;
		cin >> n;
		if (n == 0) break;
		for (int i = 0; i < n; i++)
		{
			int input;
			cin >> input;
		}
		cout << "Case "<< ca++ << ": Sorting... done!\n";
	}

	return 0;
}