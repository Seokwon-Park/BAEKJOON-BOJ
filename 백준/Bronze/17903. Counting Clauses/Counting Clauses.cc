#include <bits/stdc++.h>

using namespace std;

using vi = vector<int>;
using vd = vector<double>;
using ll = long long;
using vll = vector<ll>;
template <typename T>
using vec = vector<T>;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	int n, m;
	cin >> m >> n;
	for (int i = 0; i < m; i++)
	{
		int x1, x2, x3;
		cin >> x1 >> x2 >> x3;
	}
	if (m < 8) cout << "unsatisfactory";
	else cout << "satisfactory";

	return 0;
}